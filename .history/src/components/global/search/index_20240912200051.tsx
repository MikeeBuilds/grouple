import React from 'react'

type Props = {
    className?: string
    inputStyle?: string
    placeholder?: string
    searchType?: string
    iconStyle?: string
}

const Search = (props: Props) => {
  return (
    <div>Search</div>
  )
}

export default Search