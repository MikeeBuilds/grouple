import React from "react"

type Props = {
  className?: string
  inputStyle?: string
  placeholder?: string
  searchType: "GROUPS"
  iconStyle?: string
  glass?: boolean
}

const Search = (props: Props) => {
  return <div>Search</div>
}

export default Search
