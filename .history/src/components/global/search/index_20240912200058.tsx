import React from "react"

type Props = {
  className?: string
  inputStyle?: string
  placeholder?: string
  searchType?: string
  iconStyle?: string
  glas?: boolean
}

const Search = (props: Props) => {
  return <div>Search</div>
}

export default Search
