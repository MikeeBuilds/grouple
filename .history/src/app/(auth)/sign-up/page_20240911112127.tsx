"use client"
type Props = {}

const SignUpForm = (props: Props) => {
    const {
        register,
        errors,
        verifying,
        creating,
        onGenerateCode,
        onInputChange,
    }

    return <div>SignUpForm</div>
}

export default SignUpForm
