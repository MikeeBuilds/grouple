import React from "react"

type Props = {
  id: string
  name: string
  category: string
  createdAt: string
  userId: string
  privacy: string
  thumbnail: string
  description: string
  preview: string
}

const GroupCard = (props: Props) => {
  return <div>GroupCard</div>
}

export default GroupCard
