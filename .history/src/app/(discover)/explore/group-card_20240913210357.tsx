import React from 'react'

type Props = {
    id: string
    createdAt: string
    userId: string
    category: string
    privacy: string
    thumbnail: string
    description: string
    name: string
    preview: string
}

const GroupCard = (props: Props) => {
  return (
    <div>GroupCard</div>
  )
}

export default GroupCard