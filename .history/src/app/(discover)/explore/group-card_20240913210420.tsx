import React from 'react'

type Props = {
    id: string
    name: string
    category: string
    createdAt: string
    userId: string
    thumbnail: string
    privacy: string
    description: string
    preview: string
}

const GroupCard = (props: Props) => {
  return (
    <div>GroupCard</div>
  )
}

export default GroupCard