import React from 'react'

type Props = {
    id: string
    createdAt: string
    userId: string
    category: string
    description: string
    privacy: string
    thumbnail: string
    name: string
    preview: string
}

const GroupCard = (props: Props) => {
  return (
    <div>GroupCard</div>
  )
}

export default GroupCard