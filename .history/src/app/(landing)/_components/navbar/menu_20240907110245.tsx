'use client'

type MenuProps = {
    orientation: "moblile" | "desktop"
}

type Props = { orientation: MenuProps}

const Menu = (props: Props) => {
  return (
    const { secrion, onSetSection } 
    <div>Menu</div>
  )
}

export default Menu